while True:
    try:
        arr = list(map(str,input().split(" ")))
        x = []
        for itm in arr:
            x.append(itm[::-1])
        #or i in x:
          # res = ''.join(i)
       #print(res, end = ' ')
        print(*x)
    except:
        break
        
        
        
